# OmniGit 版本变更记录 (Changelog)

本项目遵循 [Semantic Versioning (语义化版本规范)](https://semver.org/lang/zh-CN/)，所有的版本更新与变更记录均在此维护。

---

## [v0.4.0] - 2026-09-24

> **版本定位**：核心稳定性突破与上手体验全面升级。彻底解决冷启动工作区加载、路径大小写匹配及弹窗层级问题，首创 0ms 瞬时水合秒开，并附带针对非 IDEA 用户的完整图文实战教程。

### 🐛 缺陷修复 (Bug Fixes)

- **彻底修复冷启动打开工作空间显示空白的严重缺陷**：
  - **根本诱因**：开机/冷启动时后台异步扫描项目返回 Windows 标准大写盘符（如 `D:\projects\...`），而本地存储的工作区路径为小写盘符（如 `d:\projects\...`）。在工作区项目过滤解析时使用了严格全等比较（`===`），导致仓库被误过滤为空数组。
  - **误删工作区防护**：修复了因仓库列表为空而误触发 `removeSavedWorkspace` 并清空项目、分支、文件改动状态的破坏性回退逻辑。
  - **跨平台路径归一化**：新增并全面应用 `normalizePath()` 工具函数，抹平 Windows 盘符大小写差异、统一正反斜杠及去除尾端斜杠，确保无论路径如何输入均能 100% 精确匹配。
  - **弹窗勾选防重修复**：修复了 `AddRepoModal`（添加仓库弹窗）中已存在仓库的比对与勾选判定逻辑。
- **修复弹窗层叠上下文被顶部导航栏裁剪问题**：
  - 对 `ConfirmDialog`（全局安全确认弹窗）、新建分支弹窗、分支重命名弹窗及检出版本弹窗全面接入 React Portal，脱离局部 DOM 树独立渲染，彻底解决层级遮挡与被容器截断的 UI 缺陷。
- **修复自动更新检查源地址**：
  - 修正了 `updater.ts` 内部对接的 GitHub Releases API 仓库路径，确保在线检测更新与版本升级通道畅通。

### 🚀 体验与性能优化 (Performance & UX)

- **0ms 瞬时水合秒开机制（Instant Hydration）**：
  - 在欢迎屏幕点击“打开工作空间”或单仓库的同一渲染周期，立即利用卡片元数据及本地 L1 内存 / L2 存储快照挂载全部仓库与分支列表，首屏秒开无白屏、无闪烁，无需等待异步状态接口返回即可直接看到完整仓库结构。
- **“一键撤销合并 (Undo Merge)”安全回滚体验升级**：
  - 顶栏分支菜单中的“撤销合并”功能改为常态化常驻；
  - 增加动态合并节点嗅探，智能识别最近一次 `git merge` 历史，并在回退前弹出高风险操作二次确认对话框，防止手滑误操作。
- **大容量多仓库聚合更稳健**：
  - 增强 `setWorkspaceProjects` 与 `addProjectsToWorkspace` 的容错映射，保证多工程工作区中的每一个仓库都被完整呈现，绝不发生仓库遗漏。

### 📚 文档与新手支持 (Documentation & Guides)

- **新增官方零门槛图文实操教程指南**：
  - 针对从未使用过 IntelliJ IDEA、习惯轻量编辑器（VS Code / Sublime）或终端命令行的开发者，在 `docs/` 下编写发布了《OmniGit 极速上手与三方冲突合并实战指南》（图文并茂、大纲导航、冲突模拟演练）。
  - 配套开源了一键自动化生成 Word 教程的 Python 工具脚本。
- **官方文档与截图脱敏规范化**：
  - 重新设计并替换官方预览图，去除内网或私有项目路径，换用规范的 OmniGit 演示工程。
- **开源合规与社区化规范建设**：
  - 正式发布标准 MIT 开源授权协议文件（`LICENSE`）。
  - 增加标准化 GitHub Issue 模版（Bug 报告、新特性提议）与 Pull Request 模版。
  - 添加 `.gitattributes`，纠正 GitHub 仓库源码语言统计偏差。

### 📦 客户端全平台打包与分发 (Packaging & Release)

- 全面升级客户端版本定义至 `0.4.0`，包含 `package.json`、前端常量 `version.ts` 及打包脚本。
- 完善 Windows 绿色便携包（ZIP）、单文件安装包（Setup.exe，彻底免 UAC 提权）以及 macOS（Apple Silicon arm64 / Intel x64）双架构独立发布包编译脚本。

---

## [v0.3.0] - 2026-09-18

- 引入全新设计的“工作空间管理欢迎界面 (Welcome Screen)”。
- 支持按公共父目录智能聚合多代码仓库为独立工程工作区。
- 实现 L1 内存 / L2 LocalStorage / L3 本地磁盘文件三级快照缓存机制，工程切换速度提升 10 倍。
- 支持暗色模式 (Darcula) 与亮色模式 (IDEA Light) 主题自由切换。
- 支持中英文双语界面无缝切换。

---

## [v0.2.0] - 2026-09-12

- 实现独立原生级 3-Way Merge 可视化冲突解决合并器。
- 支持双向箭头智能采纳或丢弃修改，实时高亮对比。
- 增加未解决冲突标记 (`<<<<<<<`) 拦截防漏提机制。
- 完善 Git 提交面板、追加提交 (Amend) 与单文件 Diff 比对器。

---

## [v0.1.0] - 2026-09-05

- OmniGit 首次开源发布，完成 Electron 桌面客户端核心骨架。
- 支持多仓库列表切换、本地分支浏览与签出。
- 本地沙箱离线回环执行，保护代码资产绝对安全。
